# MedClaim 最小评测器设计

日期：2026-09-01  
状态：按用户“无需确认、可完全代理”授权执行

## 目标

把现有 10 条人工金标准变成一个可运行、可复核的作品集闭环：给定模型预测与六维人工评分，计算分类和安全指标，并生成可直接阅读的报告。

## 输入与输出

输入：

- `medclaim-eval-cases-v0.1.csv`：`case_id`、类别和 `gold_label`；
- `medclaim-predictions-baseline.csv`：每例预测标签、六维 0–2 分、严重失败标记、失败根因、理由和证据定位。

输出：

- 终端摘要；
- `medclaim-results-baseline.json`：机器可读指标；
- `medclaim-results-baseline.md`：可读基线报告。

## 处理流程

1. 用 Python 标准库读取两个 CSV。
2. 在计算前拒绝重复编号、缺失/多余病例、非法标签、越界评分和非法布尔值。
3. 按 `case_id` 合并金标准与预测。
4. 计算准确率、四分类 macro-F1、各标签 precision/recall/F1、严重失败数/率、六维平均分、Rubric 通过数/率和按错误类别准确率。
5. 依据 Rubric：总分至少 80、标签正确且无严重失败才算通过。
6. 写出 JSON 与 Markdown；结果内注明这是 10 例校准集，不能代表外部泛化性能。

## 验收条件

- 一条命令可运行；仅依赖 Python 标准库。
- 10 个 case 必须一一对应，任何输入错误均以非零状态退出并给出具体原因。
- 权重固定为 Rubric v0.1 的 25/20/15/20/10/10，总和为 100。
- 自检覆盖：完美预测应得到 accuracy=1、macro-F1=1、严重失败率=0、通过率=1；缺失病例必须失败。
- 输出不得包含真实患者数据或个体化诊疗建议。

## 非目标

- 不调用任何大模型或外部 API；
- 不做 RAG、Dify 工作流、数据库、Web UI 或部署；
- 不把 10 例结果包装成模型临床有效性；
- 不引入 pandas、scikit-learn 或测试框架。

## 停止条件

上述验收通过即停止。本轮不提前建设 50 例版本或界面；等真实基线暴露需求后再扩展。
