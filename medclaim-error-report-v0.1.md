# MedClaim 多数类规则基线错误报告 v0.1

## 结论

固定输出“不支持”得到 50.0% Accuracy，但 Macro-F1 仅 0.1667、Rubric 通过率为 0%。这说明准确率主要来自类别分布，而不是证据理解；它只能作为管线下限，不能称为医疗 AI 模型结果。

## 错误分布

| 金标准 | 样本数 | 判对数 | 主要错误 |
|---|---:|---:|---|
| 支持 | 2 | 0 | 两例都被错误拒绝 |
| 部分支持 | 2 | 0 | 无法区分“部分正确、部分错误” |
| 不支持 | 5 | 5 | 仅因固定标签偶然命中 |
| 证据不足 | 1 | 0 | 把“来源未报告”误判成“来源反驳” |

五个表面正确病例也没有人群、线别、数字、引用、证据边界或可审计性判断，因此总分均只有 25/100，仍未通过 Rubric。

## 不能从结果推出什么

- 严重失败率为 0% 不代表安全；该规则没有生成医学解释，几乎没有可触发的医学断言。
- 50% Accuracy 不代表有证据判读能力；对“支持”“部分支持”“证据不足”的召回率均为 0。
- 10 条开发集不能作为独立泛化证据。

## 下一轮可检验假设

在锁定的20条 holdout 上，用同一独立模型分别运行无检索与 RAG 两种条件。若 RAG 有效，应优先改善引用定位、证据边界和可审计性；若标签仍错，再按 `retrieval`、`model`、`prompt`、`tool`、`product_spec` 归因，而不是笼统归为“模型幻觉”。
