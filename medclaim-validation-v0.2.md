# MedClaim 数据与评测验证报告 v0.2

验证日期：2026-09-01  
验证对象：开发集 v0.2、Rubric v0.2、评测器和多数类规则基线

## Overall Assessment：可分享但需注明校准限制

v0.2 已消除一处会导致标签争议的规则/样本冲突，结构、配额、权重和评测计算均通过复检。独立 holdout 已建立并隔离金标准，但尚无真实模型结果，不能宣称模型性能。

## 方法复核

- 问题与目标一致：判断给定来源是否支持医学主张，不替代临床决策。
- 10 条全部是开发/校准集；明确禁止把看过金标准后的分数当独立测试结果。
- 多分句主张先拆原子主张，再按决定性矛盾、部分支持和证据不足顺序裁决。
- 严重失败评价系统回答是否识别危险错误，不把测试主张本身含错误误算为系统失败。

## 发现与处理

1. **高影响，已修复：** v0.1 的 MC003 将决定性人群外推判为“部分支持”，与裁决规则冲突。v0.2 替换成“ORR 数字正确、主要终点错误”的清晰混合主张，标签仍为部分支持。
2. **高影响，已修复：** 已建立 10 条独立 holdout；公开输入不含类别、标签或证据答案，私有金标准由 `.gitignore` 排除。
3. **中影响，待校准：** MC003、MC004、MC007 仍需间隔重评，之后才能锁定 Rubric。

## 计算复核

- 10 条记录、10 个唯一 ID、9 个必填字段完整：通过。
- 类别配额：2/2/2/1/2/1，与设计一致；金标准标签 2/2/5/1，与 v0.1 一致。
- Rubric 六维权重：25+20+15+20+10+10=100：通过。
- v0.1→v0.2 逐行比较：只有 MC003 发生变化：通过。
- 完美预测自检：Accuracy、Macro-F1、通过率均为 1，严重失败率为 0：通过。
- 缺失病例：评测器拒绝运行：通过。
- 多数类规则基线：Accuracy 0.5000、Macro-F1 0.1667、通过率 0、平均分 12.50/100：独立复算一致。
- 失败根因字段：非法值被拒绝；当前 10 个规则基线失败均为 `not_applicable`：通过。
- Holdout：10 个 prompt 与 10 个私有 gold ID/主张/来源一一对应；公开输入无标签字段；与开发集最高主张相似度 0.640；类别配额一致：通过。

## 限制与下一门槛

- 当前类别是人为均衡/压力测试设计，不代表真实工作流分布。
- 数据集中在 HER2 阳性胃/GEJ 癌与 T-DXd，不能外推到其他疾病、药物或监管地区。
- 完成三例间隔重评后，可让独立模型仅读取 holdout prompts，进入基础提示词与 RAG 版本对比。
