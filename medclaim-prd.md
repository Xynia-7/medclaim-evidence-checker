# MedClaim Evidence Checker｜一页 PRD

版本：0.2
日期：2026-09-01

## 1. 问题与用户

医学编辑、医学运营和内容审核人员需要快速判断“一条医疗主张及其引用是否匹配”。通用聊天模型可能遗漏 HER2 状态、治疗线别、研究设计和关键数字，也可能把研究结果写成获批适应证。

**目标用户：** 具备基础医学阅读能力的内容审核人员。  
**核心任务：** 输入主张和公开来源，得到可复核的支持关系、最小证据、人群/线别和边界提示。  
**成功定义：** 用户能在 2 分钟内复核系统判断，并知道何时必须升级给医学专家。

## 2. 本期范围

只处理 HER2 阳性胃癌/胃食管交界部癌与抗体偶联药物相关主张；只使用 FDA、NCI、EMA、PubMed 摘要和合法开放获取全文。

不处理：真实患者资料、诊断、处方、个体化治疗决策、影像判读、付费论文全文再分发和自动对外发布。

## 3. 用户流程

1. 用户提交 `medical_claim` 与 `source_url`。
2. 系统检查来源是否允许、是否可访问、是否含目标主题。
3. 系统检索与主张最相关的最小证据片段。
4. 系统输出四分类标签、理由、证据定位、人群/线别和边界。
5. 命中严重风险或证据不足时，系统标记“人工复核”，不替用户做临床结论。

## 4. 输出契约

| 字段 | 要求 |
|---|---|
| `label` | 仅限支持、部分支持、不支持、证据不足 |
| `reason` | 1–3 句，说明决定标签的关键事实 |
| `evidence_locator` | 来源标题、章节/页码及最小证据定位 |
| `population_line` | 癌种、HER2、阶段、既往治疗和线别 |
| `boundary` | 区分监管适应证、试验结果、综述或未知 |
| `needs_human_review` | 严重风险或证据不足时为 `true` |

## 5. 验收标准

- 输入与输出可在同一病例上完整追溯；
- 开发集与隐藏 holdout 分离，模型不得接触 holdout 金标准；
- 对每个版本报告 Accuracy、Macro-F1、Rubric 通过率、严重失败率及分类型结果；
- 任何虚构来源、适应证扩张、决定性人群/线别错配或关键数字篡改均阻断通过；
- RAG 版除最终标签外，还必须报告检索是否命中正确证据；
- 输出不能包含个体化诊疗建议。

## 6. 失败归因

每个失败只选一个首要根因，再记录可选次要原因：

1. `prompt`：指令或输出契约不清；
2. `retrieval`：未找到正确证据或找到错误片段；
3. `model`：证据正确但推理/转述错误；
4. `tool`：解析、接口或数据处理错误；
5. `product_spec`：任务定义、金标准或升级规则不清。

## 7. 人工升级规则

证据冲突、来源版本不明、标签置信不足、涉及真实患者、可能改变治疗决策或触发严重失败条件时，一律停止自动结论并交医学专家复核。系统不显示“安全可用”之类临床承诺。

## 8. 里程碑

- v0.1：10 条开发集、Rubric、标准库评测器和多数类规则基线；
- v0.2：Rubric 锁定、独立 holdout、基础提示词模型基线；
- v0.3：公开资料检索层、三次无检索/RAG 对比和 AI 预复核已完成；
- v1.0：50 条数据、错误分析、结果报告、产品指标卡和 3 分钟演示已完成；待第二名人类评分者和20例计时实验。

产品指标的公式、分母、前瞻性目标和发布门槛见 [`medclaim-workflow-metrics.md`](medclaim-workflow-metrics.md)。
